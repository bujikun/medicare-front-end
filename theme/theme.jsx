import { extendTheme } from "@chakra-ui/theme-utils";

export const theme = extendTheme({
    styles: {
        global: {
            body: {
                bg: "#eee",
                color:"#333"
            }
        }
    }
});